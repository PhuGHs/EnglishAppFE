import React from 'react';
import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const QAsScreen = () => {
    return (
        <SafeAreaView>
            <Text>This is QA screen</Text>
        </SafeAreaView>
    );
};

export default QAsScreen;